name: Test Heredoc
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat > /tmp/test.py << 'EOF'
print("hello world")
EOF
          python3 /tmp/test.py
